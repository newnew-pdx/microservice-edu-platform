server:
  port: 8084

spring:
  application:
    name: edu-learning-service
  data:
    redis:
      # Step9 使用 Redis 保存学习进度、签到 Bitmap 和积分排行榜。
      host: localhost
      port: 6379
      connect-timeout: 1s
      timeout: 2s
  cloud:
    nacos:
      discovery:
        # 本阶段只使用 Nacos Discovery，不启用配置中心。
        server-addr: localhost:8848

learning:
  sign:
    points: 10
